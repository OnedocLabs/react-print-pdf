---
title: compile
icon: code
---

Compile a React component to a string with the Onedoc print styles.

## compile

### Examples

#### Emotion CSS

Pass `{ emotion: true }` as the second compile option to merge and extract critical CSS using Emotion. Some libraries such as Chakra UI require this option to work correctly.

```jsx
const html = await compile(<Component />, { emotion: true });
```

<Frame type="glass"><img src="../images/previews/compile-b4572149/document.1.jpg" style={{ maxHeight: '400px', borderRadius: "0.25rem", overflow: "hidden" }} /></Frame>

<div style={{paddingTop: "1rem", paddingBottom: "1rem"}}><CodeGroup>
```jsx template.tsx
import { compile } from "@onedoc/react-print";
import { Button, ChakraProvider, extendTheme } from "@chakra-ui/react";

<>
  <ChakraProvider>
    <Button colorScheme="blue">Hello</Button>
  </ChakraProvider>
</>;

```
```css base.css
@import url("https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap");

html,
body {
  font-size: 28px;
  font-family: "Inter", sans-serif;
}

@page {
  size: A4;
}

```
</CodeGroup></div>

